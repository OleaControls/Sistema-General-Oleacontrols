{
  "rules": [
    {
      "allowed": {
        "origins": [
          "https://sistema-general-oleacontrols.vercel.app",
          "http://localhost:5173"
        ],
        "methods": ["PUT", "GET", "HEAD"],
        "headers": ["content-type"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
